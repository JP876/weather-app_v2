{
    "name": "weather-app",
    "private": true,
    "scripts": {
        "start": "node apps/api/dist/index",
        "build": "turbo run build",
        "dev": "turbo run dev",
        "lint": "turbo run lint"
    },
    "devDependencies": {
        "prettier": "^3.8.0",
        "turbo": "^2.7.5"
    },
    "engines": {
        "node": ">=18"
    },
    "packageManager": "npm@11.6.2",
    "workspaces": [
        "apps/*"
    ]
}
